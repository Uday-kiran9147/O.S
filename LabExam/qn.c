#include<stdio.h>
#include<unistd.h>
// #include<pthread.h>

int req_resourse(int cid, int req[]);

int req_resourse(int cid, int req[]);

int main(){
    int n,m;
    pid_t a;
    int req1[5];
    int req2[8];
    req_resourse(7,req1);

}